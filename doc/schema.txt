TypeBateau:
nomBateau(clef)
taille
---------------------------------------------------------------------------------
Bateau:
idBateau(clef)
idPartie(clef)
pseudo(clef)
nomBateau(clef)
pivot
orientation
pointVie
---------------------------------------------------------------------------------
Partie:
idPartie(clef)
dateDemarage
---------------------------------------------------------------------------------
Joueur:
pseudo(clef)
nom
prenom
dateNaissance
adrRue
adrNumero
adrCodePostal
adrCodePostal
adrVille
---------------------------------------------------------------------------------
Action:
idAction(clef)
idPartie(clef)
idBateau(clef)
tour
---------------------------------------------------------------------------------
ActionDeplacement:
idAction(clef)
idPartie(clef)
tour
typeDeplacement
---------------------------------------------------------------------------------
ActionTir:
idAction(clef)
idPartie(clef)
tour
cibleTir
---------------------------------------------------------------------------------
Vainqueurs:
idPartie(clef)
pseudo
---------------------------------------------------------------------------------
JouersParPartie:
pseudo(clef)
idPartie(clef)
